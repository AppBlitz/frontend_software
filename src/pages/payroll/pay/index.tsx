import SeeAllPayrolls from "../../../components/payroll_components/buttons/seeAllPayrolls"
const Pay: React.FC = () => {
  return (
    <div>
      <SeeAllPayrolls />
    </div>
  ); // El componente solo renderiza un div vacío
};

export default Pay;
